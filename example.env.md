--Below is what the .env will look like just use this command and follow the directions in secret key--
```
pip install python-dotenv
```

SECRET_KEY = bytes.fromhex('run python generatehexkey.py and paste teh result here')
URL = 'http://127.0.0.1:5000/weather'